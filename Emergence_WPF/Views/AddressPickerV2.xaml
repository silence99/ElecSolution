<Window x:Class="Emergence_WPF.Views.AddressPickerV2"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="AddressPickerV2" Height="300" Width="300"
        WindowState="Normal"
        WindowStyle="None"
        Topmost="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        <Canvas>
            <Border Name="MapContainer" Margin="0,0,0,0"/>   
        </Canvas>
        <DockPanel Grid.Row="1" Grid.Column="0">            
            <Button Content="取消" Click="CancelButton_Click" Width="50" Margin="10,0,5,0" DockPanel.Dock="Right"/>
            <Button Content="确定" Click="OKButton_Click" HorizontalAlignment="Right" Width="50" Margin="10,0,5,0" DockPanel.Dock="Right"/>
        </DockPanel>
    </Grid>
</Window>
